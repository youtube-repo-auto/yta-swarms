# LLM SDKs (direct, no swarms dependency)
anthropic>=0.40.0
openai>=1.50.0          # kept for any remaining OpenAI calls (e.g. Whisper if added)

# Supabase
supabase>=2.0.0

# Data validation
pydantic>=2.5.0

# Environment variables
python-dotenv>=1.0.0

# YouTube Data API v3
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-httplib2>=0.2.0

# HTTP / search
requests>=2.31.0
duckduckgo-search>=6.0.0   # Free fallback; optional if SerpAPI is used

# Utilities (already stdlib, listed for clarity)
# json, logging, pathlib, argparse → stdlib, no install needed

ddgs
